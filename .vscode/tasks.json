{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build",
			"command": "${workspaceFolder}\\misc\\build.bat",
			"options": {
				"cwd": "${workspaceFolder}"
			},
		}
		{
			"type": "shell",
			"label": "run",
			"command": "${workspaceFolder}\\misc\\run.bat",
			"options": {
				"cwd": "${workspaceFolder}"
			},
		}
	]
}